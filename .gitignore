# secrets — NEVER commit
.env
.env.local
.env.*.local

# browser sessions (contain auth cookies) and scraped financial data
profiles/
data/

# deps & logs
node_modules/
*.log
npm-debug.log*

# workflow-suite scratch (design specs are drafts to intake into NKS, not repo state)
docs/superpowers/

# machine-local Claude Code settings (project settings.json IS committed)
.claude/settings.local.json

# Wrangler local cache/state (relay)
.wrangler/
