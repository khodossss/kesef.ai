{
  "name": "kesef-connector",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "User-assisted (manual login) MCP connector for Israeli banks — native, system Chrome, read-only",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "check": "node tools/check-syntax.mjs",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test test/**/*.test.mjs",
    "verify": "npm run check && npm run lint && npm run format:check && npm test",
    "prepare": "git config core.hooksPath .githooks || echo \"enable the pre-commit gate manually: git config core.hooksPath .githooks\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.19.1",
    "israeli-bank-scrapers-core": "^6.1.4",
    "puppeteer-core": "^24.10.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.28.0",
    "eslint": "^9.28.0",
    "globals": "^15.15.0",
    "prettier": "^3.5.3"
  }
}
